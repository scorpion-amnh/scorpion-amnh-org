import { Icon } from "@/app/components/icons/Icon";

export const ExternalLinkIcon = ({ className }: { className?: string } = {}) => (
  <Icon name="arrow-up-right-from-square" inline className={className} />
);
